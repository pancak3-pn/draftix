# HTML must always be revalidated so visitors get fresh chunk references
# after a deploy; hashed assets are immutable and cache forever.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

/*
  Cache-Control: public, max-age=0, must-revalidate
